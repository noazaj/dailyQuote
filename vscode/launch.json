{
    "version": "0.2.0",
    "configurations": [
      {
        "type": "go",
        "request": "launch",
        "name": "Launch current package",
        "mode": "auto",
        "program": "${fileDirname}",
        "cwd": "${workspaceFolder}"
      }
    ]
  }
  